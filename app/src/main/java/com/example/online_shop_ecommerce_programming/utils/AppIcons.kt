package com.example.matule.utils

import androidx.compose.runtime.Composable
import androidx.compose.ui.res.painterResource
import com.example.matule.R

// Иконки используемые в приложении
object AppIcons {
    // Иконка с открытым глазом
    @Composable
    fun VisibleOn() = painterResource(R.drawable.ic_visible_on)

    // Иконка с закрытым глазом
    @Composable
    fun VisibleOff() = painterResource(R.drawable.ic_visible_off)

    // Иконка с закрытым глазом
    @Composable

    fun Bag() = painterResource(R.drawable.ic_bag)
    // Иконка с открытым глазом
    @Composable
    fun Cart() = painterResource(R.drawable.ic_cart)

    // Иконка с закрытым глазом
    @Composable
    fun EmailOtp() = painterResource(R.drawable.ic_email_otp)

    // Иконка с закрытым глазом
    @Composable
    fun FavoriteOutline() = painterResource(R.drawable.ic_favorite_outline)

    // Иконка с закрытым глазом
    @Composable
    fun BlueNike() = painterResource(R.drawable.blue_nike)

    // Иконка с закрытым глазом
    @Composable
    fun OrangeNike() = painterResource(R.drawable.orange_nike)

    // Иконка с закрытым глазом
    @Composable
    fun VioletNike() = painterResource(R.drawable.violet_nike)

    // Иконка с закрытым глазом
    @Composable
    fun Plus() = painterResource(R.drawable.ic_plus)

    // Иконка с закрытым глазом
    @Composable
    fun Minus() = painterResource(R.drawable.ic_minus)

    // Иконка с закрытым глазом
    @Composable
    fun Profile() = painterResource(R.drawable.ic_profile)

    // Иконка с закрытым глазом
    @Composable
    fun Notificaton() = painterResource(R.drawable.ic_notification)

    // Иконка с закрытым глазом
    @Composable
    fun ChevronLeft() = painterResource(R.drawable.ic_chevron_left)

    // Иконка с закрытым глазом
    @Composable
    fun ChevronDown() = painterResource(R.drawable.ic_chevron_down)

    // Иконка с закрытым глазом
    @Composable
    fun Menu() = painterResource(R.drawable.ic_menu)

    // Иконка с закрытым глазом
    @Composable
    fun Sale() = painterResource(R.drawable.sale)

    // Иконка с закрытым глазом
    @Composable
    fun Search() = painterResource(R.drawable.search)

    // Иконка с закрытым глазом
    @Composable
    fun BlueNike2() = painterResource(R.drawable.blue_nike_2)

    // Иконка с закрытым глазом
    @Composable
    fun WhiteBlueNike() = painterResource(R.drawable.white_blue_nike)

    // Иконка с закрытым глазом
    @Composable
    fun BlackNike() = painterResource(R.drawable.black_nike)

    // Иконка с закрытым глазом
    @Composable
    fun RedNike() = painterResource(R.drawable.red_nike)

    // Иконка с закрытым глазом
    @Composable
    fun HeartField() = painterResource(R.drawable.ic_heart_field)

    // Иконка с закрытым глазом
    @Composable
    fun HeartOutline() = painterResource(R.drawable.ic_heart_outline)

    // Иконка с закрытым глазом
    @Composable
    fun Shuffle() = painterResource(R.drawable.ic_shuffle)

    // Иконка с закрытым глазом
    @Composable
    fun BackgroundBNB() = painterResource(R.drawable.ic_backgorundbnb)

    // Иконка с закрытым глазом
    @Composable
    fun Home() = painterResource(R.drawable.ic_home)

    // Иконка с закрытым глазом
    @Composable
    fun BackgroundSneaker() = painterResource(R.drawable.bacground_sneaker)

    // Иконка с закрытым глазом
    @Composable
    fun Microphone() = painterResource(R.drawable.ic_microphone)

    // Иконка с закрытым глазом
    @Composable
    fun Clock() = painterResource(R.drawable.ic_clock)

    // Иконка с закрытым глазом
    @Composable
    fun BlueBlueNike() = painterResource(R.drawable.blue_blue_nike)

    // Иконка с закрытым глазом
    @Composable
    fun Shield() = painterResource(R.drawable.ic_shield)

    // Иконка с закрытым глазом
    @Composable
    fun Edit() = painterResource(R.drawable.ic_edit)

    // Иконка с закрытым глазом
    @Composable
    fun Phone() = painterResource(R.drawable.ic_phone)

    // Иконка с закрытым глазом
    @Composable
    fun Email() = painterResource(R.drawable.ic_email)

    // Иконка с закрытым глазом
    @Composable
    fun Map() = painterResource(R.drawable.map)

    // Иконка с закрытым глазом
    @Composable
    fun Location() = painterResource(R.drawable.ic_location)

    // Иконка с закрытым глазом
    @Composable
    fun Bank() = painterResource(R.drawable.bank)

    // Иконка с закрытым глазом
    @Composable
    fun Happy() = painterResource(R.drawable.happy)

    // Иконка с закрытым глазом
    @Composable
    fun RedWhiteNike() = painterResource(R.drawable.red_white)

    // Иконка с закрытым глазом
    @Composable
    fun ProfileImage() = painterResource(R.drawable.profile)

    // Иконка с закрытым глазом
    @Composable
    fun Settings() = painterResource(R.drawable.ic_settings)

    // Иконка с закрытым глазом
    @Composable
    fun Delivery() = painterResource(R.drawable.ic_delivery)

    // Иконка с закрытым глазом
    @Composable
    fun LogOut() = painterResource(R.drawable.ic_logout)

    // Иконка с закрытым глазом
    @Composable
    fun HomeScreen() = painterResource(R.drawable.home_screen)

    // Иконка с закрытым глазом
    @Composable
    fun QrBar() = painterResource(R.drawable.qr_bar)

    // Иконка с закрытым глазом
    @Composable
    fun Done() = painterResource(R.drawable.ic_done)

    // Иконка с закрытым глазом
    @Composable
    fun EditProfile() = painterResource(R.drawable.ic_edit_profile)

    // Иконка с закрытым глазом
    @Composable
    fun Delete() = painterResource(R.drawable.ic_trash)
}